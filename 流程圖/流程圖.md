```mermaid
flowchart TD

  %% ════════════════════════════════════
  %% 第一層：使用者
  %% ════════════════════════════════════
  U1(["手機 / 網頁\nPOST 請求"])
  U2(["開啟儀表板\ndoGet / 選單"])
  U3(["更新預算\n輸入金額"])
  U4(["開啟試算表\nonOpen 觸發"])

  %% ════════════════════════════════════
  %% 第二層：系統（強制同層）
  %% ════════════════════════════════════
  S1["doPost()\n接收記帳資料"]
  S2["getChartData()\n統計分析"]
  S3["askGeminiAdvisor()\n組裝 Prompt"]
  S4["updateBudget()\n寫入 + flush()"]
  S5["installableOnOpen()\n計算使用率"]

  %% 隱形連線：強制 S1~S5 同層
  S1 ~~~ S2 ~~~ S4 ~~~ S5

  %% ════════════════════════════════════
  %% 第三層：AI 模型
  %% ════════════════════════════════════
  M1[/"Gemini 2.5 Flash\ngenerateContent"/]

  %% ════════════════════════════════════
  %% 第四層：參考來源
  %% ════════════════════════════════════
  R1[("記帳明細\nappendRow")]
  R2[("getDataRange()\n讀取記帳列")]
  R3[("財務數據\n花費 / 剩餘 / 分類")]
  R4[("預算儲存格 F2\n每月預算設定值")]

  %% 隱形連線：強制 R1~R4 同層
  R1 ~~~ R2 ~~~ R3 ~~~ R4

  %% ════════════════════════════════════
  %% 第五層：輸出
  %% ════════════════════════════════════
  O1["寫入新記帳列"]
  O2["Google Charts JS\n前端圖表渲染"]
  O3["互動圖表\n圓餅 / 折線 / 長條"]
  O4["AI 財務建議\n點評 + 建議 + 冷笑話"]
  O5["預算卡片更新\n進度條 + AI 重診"]
  O6["ui.alert() 彈窗\n等級標題 + AI 點評"]

  %% 隱形連線：強制 O1~O6 同層
  O1 ~~~ O2 ~~~ O5 ~~~ O6

  %% ════════════════════════════════════
  %% 資料流（第一層 → 第二層）
  %% ════════════════════════════════════
  U1 --> S1
  U2 --> S2
  U3 --> S4
  U4 --> S5

  %% ════════════════════════════════════
  %% 資料流（第二層 → 第三層）
  %% ════════════════════════════════════
  S3 --> M1
  S2 --> S3
  S4 --> S3
  S5 --> S3

  %% ════════════════════════════════════
  %% 資料流（第二層 ↔ 第四層）
  %% ════════════════════════════════════
  S1 --> R1
  S2 <--> R2
  S3 --- R3
  S4 <--> R4

  %% ════════════════════════════════════
  %% 資料流（第三層 → 第五層）
  %% ════════════════════════════════════
  M1 --> O4
  M1 --> O6

  %% ════════════════════════════════════
  %% 資料流（第四層 → 第五層）
  %% ════════════════════════════════════
  R1 --> O1
  S2 --> O2
  O2 --> O3
  S4 --> O5

  %% ════════════════════════════════════
  %% 樣式
  %% ════════════════════════════════════
  classDef user   fill:#F1EFE8,stroke:#888780,stroke-width:1px,color:#444441
  classDef system fill:#E6F1FB,stroke:#378ADD,stroke-width:1px,color:#0C447C
  classDef model  fill:#EEEDFE,stroke:#7F77DD,stroke-width:1px,color:#3C3489
  classDef source fill:#E1F5EE,stroke:#1D9E75,stroke-width:1px,color:#085041
  classDef output fill:#FAECE7,stroke:#D85A30,stroke-width:1px,color:#712B13

  class U1,U2,U3,U4 user
  class S1,S2,S3,S4,S5 system
  class M1 model
  class R1,R2,R3,R4 source
  class O1,O2,O3,O4,O5,O6 output
