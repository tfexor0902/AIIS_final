```mermaid
flowchart LR

  %% ── 1. 節點定義（純定義，不混雜樣式） ──
  subgraph user["使用者輸入"]
    U1(["📱 手機 / 網頁<br>POST 請求"])
    U2(["📊 開啟儀表板<br>doGet / 選單"])
    U3(["💰 更新預算<br>輸入金額"])
    U4(["🚀 開啟試算表<br>onOpen 觸發"])
  end

  subgraph system["系統-功能"]
    S1["⚙️ doPost()<br>接收記帳資料"]
    S2["⚙️ getChartData()<br>統計分析"]
    S4["⚙️ updateBudget()<br>寫入 + flush()"]
    S5["⚙️ installableOnOpen()<br>計算使用率"]
    S3["⚙️ askGeminiAdvisor()<br>組裝 Prompt"]
  end

subgraph db["系統-功能"]
  R1[("📁 記帳明細工作表<br>appendRow")]
  R2[("📁 getDataRange()<br>讀取全部記帳列")]
  R3[("📁 財務數據<br>總花費 / 剩餘 / 分類")]
  R4[("📁 預算儲存格 F2<br>每月預算設定值")]
end

subgraph model["模型功能"]
  M1[/"🤖 Gemini 2.5 Flash<br>generateContent"/]
end

subgraph Output["輸出"]
  O1["📝 寫入新記帳列"]
  O2["📊 Google Charts JS<br>前端圖表渲染"]
  O3["📈 互動圖表<br>圓餅 / 折線 / 長條"]
  O5["💳 預算卡片更新<br>進度條 + AI 重診"]
  O6["🚨 ui.alert() 彈窗<br>等級標題 + AI 點評"]
  O4["💡 AI 財務建議<br>點評 + 建議 + 冷笑話"]
end

  %% ── 2. 嚴格的由上至下流程連線 ──

  %% 流程 A：記帳輸入
  U1 --> S1 --> R1 --> O1

  %% 流程 B：儀表板載入與圖表
  U2 --> S2 --> R2
  S2 --> O2 --> O3

  %% 流程 C：預算管理
  U3 --> S4 --> R4
  S4 --> O5

  %% 流程 D：AI 分析核心 (集中向下游走)
  S2 --> S3
  S4 --> S3
  U4 --> S5 --> S3
  R3 --> S3
  
  S3 --> M1
  M1 --> O4
  M1 --> O6

  %% ── 3. 樣式定義與綁定（獨立宣告，避免語法衝突） ──
  classDef user    fill:#F1EFE8,stroke:#888780,color:#444441,stroke-width:2px;
  classDef system  fill:#E6F1FB,stroke:#378ADD,color:#0C447C,stroke-width:2px;
  classDef source  fill:#E1F5EE,stroke:#1D9E75,color:#085041,stroke-width:2px;
  classDef model   fill:#EEEDFE,stroke:#7F77DD,color:#3C3489,stroke-width:2px;
  classDef output  fill:#FAECE7,stroke:#D85A30,color:#712B13,stroke-width:2px;

  class U1,U2,U3,U4 user;
  class S1,S2,S3,S4,S5 system;
  class R1,R2,R3,R4 source;
  class M1 model;
  class O1,O2,O3,O4,O5,O6 output;
