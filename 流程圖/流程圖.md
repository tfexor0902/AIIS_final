```mermaid
flowchart TD

  %% ── 節點定義 ──

  U1(["手機 / 網頁\nPOST 請求"])
  U2(["開啟儀表板\ndoGet / 選單"])
  U3(["更新預算\n輸入金額"])
  U4(["開啟試算表\nonOpen 觸發"])

  subgraph GAS ["Google Apps Script（後端）"]
    S1["doPost()\n接收記帳資料"]
    S2["getChartData()\n統計分析"]
    S4["updateBudget()\n寫入 + flush()"]
    S5["installableOnOpen()\n計算使用率"]
    S3["askGeminiAdvisor()\n組裝 Prompt"]
  end

  subgraph AI ["AI 模型"]
    M1[/"Gemini 2.5 Flash\ngenerateContent"/]
  end

  subgraph SRC ["參考來源（Google Sheets）"]
    R1[("記帳明細工作表\nappendRow")]
    R2[("getDataRange()\n讀取全部記帳列")]
    R3[("財務數據\n總花費 / 剩餘 / 分類")]
    R4[("預算儲存格 F2\n每月預算設定值")]
  end

  subgraph OUT ["輸出"]
    O1["寫入新記帳列"]
    O2["Google Charts JS\n前端圖表渲染"]
    O3["互動圖表\n圓餅 / 折線 / 長條"]
    O5["預算卡片更新\n進度條 + AI 重診"]
    O6["ui.alert() 彈窗\n等級標題 + AI 點評"]
    O4["AI 財務建議\n點評 + 建議 + 冷笑話"]
  end

  %% ── 核心技巧：使用隱形連線強制 subgraph 從上到下排隊 ──
  U1 ~~~ GAS ~~~ SRC ~~~ AI ~~~ OUT

  %% ── 記帳輸入流程 ──
  U1 --> S1
  S1 --> R1
  R1 --> O1

  %% ── 儀表板載入流程 ──
  U2 --> S2
  S2 --> R2
  R2 -.-> S2  %% 改為虛線單向，避免雙向箭頭弄亂布局
  S2 --> S3

  %% ── AI 分析流程 ──
  R3 --> S3
  S3 --> M1
  M1 --> O4

  %% ── 圖表渲染流程 ──
  S2 --> O2
  O2 --> O3

  %% ── 預算管理流程 ──
  U3 --> S4
  S4 --> R4
  S4 --> O5
  S4 --> S3

  %% ── 開啟警示流程 ──
  U4 --> S5
  S5 --> S3
  M1 --> O6

  %% ── 樣式設定 ──
  classDef user    fill:#F1EFE8,stroke:#888780,color:#444441
  classDef system  fill:#E6F1FB,stroke:#378ADD,color:#0C447C
  classDef model   fill:#EEEDFE,stroke:#7F77DD,color:#3C3489
  classDef source  fill:#E1F5EE,stroke:#1D9E75,color:#085041
  classDef output  fill:#FAECE7,stroke:#D85A30,color:#712B13

  class U1,U2,U3,U4 user
  class S1,S2,S3,S4,S5 system
  class M1 model
  class R1,R2,R3,R4 source
  class O1,O2,O3,O4,O5,O6 output
