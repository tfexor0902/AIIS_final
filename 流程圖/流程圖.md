```mermaid
flowchart TD

  %% ═════════════════════════════════════════════
  %% 1. 左側垂直分層標籤軌道（橫向對照使用）
  %% ═════════════════════════════════════════════
  subgraph LABELS ["層級標記"]
    l0(["⓪ 參考來源"]) ~~~ L1(["① 使用者端"]) ~~~ L2["② 系統處理"] ~~~ L3[/"③ AI 模型"/] ~~~ L4[("④ 資料庫層")] ~~~ L5["⑤ 前端輸出"]
  end

  %% ═════════════════════════════════════════════
  %% 2. 獨立垂直流水線：A. 日常記帳自動化（已將初步寫入預算移至最前）
  %% ═════════════════════════════════════════════
  subgraph LINE_A ["【流程 A】日常自動記帳"]
    direction TB
    YT_REF(["📺 參考來源<br>YouTube 記帳教學影片"])
    click YT_REF "https://www.youtube.com/watch?v=XfIg6opVuYA" "前往觀看教學影片" _blank
    U5(["🎯 使用者<br>初步寫入預算"])
    U1(["📱 手機 / 網頁<br>POST 請求"])
    S1["⚙️ doPost()<br>接收記帳資料"]
    R1[("📁 記帳明細工作表<br>appendRow")]
    O1["📝 寫入新記帳列"]

    %% 資料流向：初始寫入預算作為最前置動作
    U5 -.->|初始環境建置| U1
    U1 --> S1 --> R1 --> O1
  end

  %% ═════════════════════════════════════════════
  %% 3. 獨立垂直流水線：B. 儀表板與圖表渲染（已拔除『剩餘』）
  %% ═════════════════════════════════════════════
  subgraph LINE_B ["【流程 B】數據視覺化"]
    direction TB
    U2(["📊 開啟儀表板<br>doGet / 選單"])
    S2["⚙️ getChartData()<br>統計分析"]
    R3_B[("📁 財務數據<br>總花費 / 分類佔比")]
    O2["📊 Google Charts JS<br>前端圖表渲染核心"]
    O3["📈 互動圖表<br>圓餅 / 折線 / 長條"]
    O7["🔍 搜尋欄 & 點擊監聽<br>前端篩選 / 查看明細彈窗"]

    U2 --> S2
    R3_B -->|讀取| S2
    S2 --> O2 --> O3
    O3 <-->|Event Listener<br>本地篩選與渲染| O7
  end

  %% ═════════════════════════════════════════════
  %% 4. 獨立垂直流水線：C. 預算動態管理（已移除 U5）
  %% ═════════════════════════════════════════════
  subgraph LINE_C ["【流程 C】預算配置調整"]
    direction TB
    U3(["💰 更新預算<br>輸入金額"])
    S4["⚙️ updateBudget()<br>寫入 + flush()"]
    R3_C[("📁 財務數據<br>預算儲存格")]
    O5["💳 預算卡片更新<br>進度條顯示"]

    U3 --> S4
    S4 -->|寫入| R3_C
    S4 --> O5
  end

  %% ═════════════════════════════════════════════
  %% 5. 獨立垂直流水線：D. 開檔 AI 主動診斷與手動重審機制
  %% ═════════════════════════════════════════════
  subgraph LINE_D ["【流程 D】AI 財政盲審警示"]
    direction TB
    U4(["🚀 開啟試算表<br>onOpen 觸發"])
    U6(["🔄 前端 UI 介面<br>點擊『重新檢視』按鈕"])
    
    S5["⚙️ installableOnOpen()<br>自動計算使用率"]
    S3["⚙️ askGeminiAdvisor()<br>組裝 Prompt"]
    R3_D[("📁 財務數據<br>讀取財務快照")]
    M1[/"🤖 Gemini 2.5 Flash<br>generateContent"/]
    O4["💡 AI 財務建議<br>點評 + 建議 + 冷笑話"]

    %% 自動流
    U4 --> S5 --> S3
    
    %% 手動流
    U6 -->|手動觸發重診| S3
    
    R3_D -->|注入 Prompt| S3
    S3 -->|呼叫| M1
    M1 --> O4
  end

  %% ═════════════════════════════════════════════
  %% 6. 跨流水線的資料同步（用虛線非同步表示）
  %% ═════════════════════════════════════════════
  R1 -.->|試算表公式自動同步| R3_B
  R3_C -.->|數據聯動| R3_D

  %% ═════════════════════════════════════════════
  %% 7. 全域視覺樣式定義
  %% ═════════════════════════════════════════════
  classDef user    fill:#F1EFE8,stroke:#888780,color:#444441,stroke-width:2px;
  classDef system  fill:#E6F1FB,stroke:#378ADD,color:#0C447C,stroke-width:2px;
  classDef source  fill:#E1F5EE,stroke:#1D9E75,color:#085041,stroke-width:2px;
  classDef model   fill:#EEEDFE,stroke:#7F77DD,color:#3C3489,stroke-width:2px;
  classDef output  fill:#FAECE7,stroke:#D85A30,color:#712B13,stroke-width:2px;
  
  classDef line    fill:#FFF,stroke:#DDD,stroke-dasharray: 5 5,color:#666;
  classDef label   fill:#F9F9F9,stroke:#333,color:#333,font-weight:bold;

  class U1,U2,U3,U4,U5,U6 user;
  class S1,S2,S3,S4,S5 system;
  class R1,R3_B,R3_C,R3_D source;
  class M1 model;
  class O1,O2,O3,O4,O5,O7 output;
  
  class LINE_A,LINE_B,LINE_C,LINE_D line;
  class LABELS label;
