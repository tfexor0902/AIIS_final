```mermaid
graph TB
    %% 節點外觀樣式定義
    classDef userLayer fill:#e6f0fa,stroke:#b1c9e8,stroke-width:1px,color:#1e467a;
    classDef sysLayer fill:#e6f5f0,stroke:#addbc8,stroke-width:1px,color:#0a5c36;
    classDef dbLayer fill:#fbf0e1,stroke:#ebd2b5,stroke-width:1px,color:#7a4b1e;
    classDef outLayer fill:#f9e9e6,stroke:#eccbc4,stroke-width:1px,color:#8a2e2b;
    classDef extLayer fill:#eee9fe,stroke:#cfc4fc,stroke-width:1px,color:#4b20a3;

    %% 1. 使用者與觸發端 (User / Input)
    subgraph Layer1 ["【 使用者 / 輸入與觸發端 】"]
        A1["<b>新增 / 記帳資料</b><br>發出 POST 請求"]:::userLayer
        A2["<b>點擊按鍵 / 記帳功能</b><br>發出 GET 請求"]:::userLayer
    end

    %% 2. 系統核心與處理端 (System / Process & Store)
    subgraph Layer2 ["【 系統 / 核心處理與儲存端 】"]
        subgraph GAS ["Google Apps Script (GAS) 後端程式"]
            B1["<b>doPost(e)</b><br>解析 JSON 並寫入"]:::sysLayer
            B2["<b>doGet(e)</b><br>載入並回傳 HTML 介面"]:::sysLayer
            B3["<b>getChartData()</b><br>撈取並轉換圖表資料"]:::sysLayer
        end
        
        C1["<b>Google 試算表核心</b><br>資料庫主體 & 負責公式運算(F2)"]:::dbLayer
        C2["<b>onOpen()</b><br>系統自動化觸發"]:::sysLayer
    end

    subgraph Layer2.5 ["【  參考來源 / 自動輸入設計來源 】"]
         E1["<b>youtube影片</b>"]:::extLayer
    end

    %% 3. 輸出與呈現端 (Output / Display)
    subgraph Layer3 ["【 輸出 / 呈現與報表端 】"]
        D1["<b>預算卡片</b><br>呈現已花費及可花費的額度"]:::outLayer
        D2["<b>圖表面板</b><br>呈現圓餅圖/折線圖/長條圖"]:::outLayer
        D3["<b>明細</b><br>呈現類別或日期的消費明細"]:::outLayer
    end

    %% 分層之間的資料流向
    A1 -->|POST| B1
    A2 -->|GET| B2
    E1 -.->|設計參考| B1
    
    B1 --> C1
    B2 --> C1
    B3 --> C1
    C2 -.->|初始化載入| C1

    C1 --> D1
    C1 --> D2
    C1 --> D3
