```mermaid
flowchart TD

  %% ── 節點定義與顏色分組 ──
  
  %% [1. 使用者觸發層]
  U1(["📱 手機 / 網頁<br>POST 請求"]) :::user
  U2(["📊 開啟儀表板<br>doGet / 選單"]) :::user
  U3(["💰 更新預算<br>輸入金額"]) :::user
  U4(["🚀 開啟試算表<br>onOpen 觸發"]) :::user

  %% [2. GAS 後端處理層]
  S1["⚙️ doPost()<br>接收記帳資料"] :::system
  S2["⚙️ getChartData()<br>統計分析"] :::system
  S4["⚙️ updateBudget()<br>寫入 + flush()"] :::system
  S5["⚙️ installableOnOpen()<br>計算使用率"] :::system
  S3["⚙️ askGeminiAdvisor()<br>組裝 Prompt"] :::system

  %% [3. Google Sheets 資料層]
  R1[("📁 記帳明細工作表<br>appendRow")] :::source
  R2[("📁 getDataRange()<br>讀取全部記帳列")] :::source
  R3[("📁 財務數據<br>總花費 / 剩餘 / 分類")] :::source
  R4[("📁 預算儲存格 F2<br>每月預算設定值")] :::source

  %% [4. AI 模型層]
  M1[/"🤖 Gemini 2.5 Flash<br>generateContent"/] :::model

  %% [5. 前端與輸出層]
  O1["📝 寫入新記帳列"] :::output
  O2["📊 Google Charts JS<br>前端圖表渲染"] :::output
  O3["📈 互動圖表<br>圓餅 / 折線 / 長條"] :::output
  O5["💳 預算卡片更新<br>進度條 + AI 重診"] :::output
  O6["🚨 ui.alert() 彈窗<br>等級標題 + AI 點評"] :::output
  O4["💡 AI 財務建議<br>點評 + 建議 + 冷笑話"] :::output

  %% ── 嚴格的由上至下流程連線 ──

  %% 流程 A：記帳輸入
  U1 --> S1 --> R1 --> O1

  %% 流程 B：儀表板載入與圖表
  U2 --> S2
  S2 --> R2
  S2 --> O2 --> O3

  %% 流程 C：預算管理
  U3 --> S4
  S4 --> R4
  S4 --> O5

  %% 流程 D：AI 分析核心 (集中向下游走)
  S2 --> S3
  S4 --> S3
  U4 --> S5 --> S3
  R3 --> S3
  
  S3 --> M1
  M1 --> O4
  M1 --> O6

  %% ── 樣式宣告（用顏色代替 subgraph 避免排版錯亂） ──
  classDef user    fill:#F1EFE8,stroke:#888780,color:#444441,stroke-width:2px;
  classDef system  fill:#E6F1FB,stroke:#378ADD,color:#0C447C,stroke-width:2px;
  classDef source  fill:#E1F5EE,stroke:#1D9E75,color:#085041,stroke-width:2px;
  classDef model   fill:#EEEDFE,stroke:#7F77DD,color:#3C3489,stroke-width:2px;
  classDef output  fill:#FAECE7,stroke:#D85A30,color:#712B13,stroke-width:2px;
